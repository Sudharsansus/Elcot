# `.github/workflows`

<!-- gen-folder-docs -->
> Folder guide for knowledge transfer — auto-generated from each file's own header documentation.
> Regenerate with `node scripts/gen-folder-docs.mjs`. Do not edit by hand (changes are overwritten); to keep a hand-written note, remove the `<!-- gen-folder-docs -->` marker line.

**Purpose:** Folder holding the files listed below (part of `.github`).

## Files (9)

| File | Type | What it does |
| --- | --- | --- |
| `ci-backend.yml` | YAML | YAML configuration. |
| `ci-cms.yml` | YAML | YAML configuration. |
| `ci-frontend.yml` | YAML | YAML configuration. |
| `config-integrity.yml` | YAML | YAML configuration. |
| `dependency-review.yml` | YAML | YAML configuration. |
| `osv-scan.yml` | YAML | YAML configuration. |
| `regen-lockfile.yml` | YAML | YAML configuration. |
| `release.yml` | YAML | YAML configuration. |
| `security-scan.yml` | YAML | YAML configuration. |

---
_Part of the Tamil Nadu AVGC-XR portal. This guide describes files as documented in their source headers; for authoritative behaviour, read the code._
