'use strict';

// Strapi 5 core controller for the Testimonial content type.
const { factories } = require('@strapi/strapi');

module.exports = factories.createCoreController('api::testimonial.testimonial');
