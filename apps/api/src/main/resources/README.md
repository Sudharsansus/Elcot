# `apps/api/src/main/resources`

<!-- gen-folder-docs -->
> Folder guide for knowledge transfer — auto-generated from each file's own header documentation.
> Regenerate with `node scripts/gen-folder-docs.mjs`. Do not edit by hand (changes are overwritten); to keep a hand-written note, remove the `<!-- gen-folder-docs -->` marker line.

**Purpose:** Resources & policy-documents feature.

## Files (5)

| File | Type | What it does |
| --- | --- | --- |
| `application-dev.yml` | YAML | Spring Boot application configuration. |
| `application-prod.yml` | YAML | Spring Boot application configuration. |
| `application-staging.yml` | YAML | Spring Boot application configuration. |
| `application.yml` | YAML | Spring Boot application configuration. |
| `logback-spring.xml` | XML | XML configuration / data. |

## Subfolders (2)

- [`db/`](db/README.md) — Folder holding the files listed below (part of `apps/api/src/main/resources`).
- [`processes/`](processes/README.md) — Folder holding the files listed below (part of `apps/api/src/main/resources`).

---
_Part of the Tamil Nadu AVGC-XR portal. This guide describes files as documented in their source headers; for authoritative behaviour, read the code._
