# `apps/api/src/main/java/in/elcot/avgcxr/platform/iam/infrastructure/persistence/entity`

<!-- gen-folder-docs -->
> Folder guide for knowledge transfer — auto-generated from each file's own header documentation.
> Regenerate with `node scripts/gen-folder-docs.mjs`. Do not edit by hand (changes are overwritten); to keep a hand-written note, remove the `<!-- gen-folder-docs -->` marker line.

**Purpose:** Folder holding the files listed below (part of `apps/api/src/main/java/in/elcot/avgcxr/platform/iam/infrastructure/persistence`).

## Files (4)

| File | Type | What it does |
| --- | --- | --- |
| `PermissionEntity.java` | Java | Permission Entity — JPA entity (maps to a database table). |
| `RoleEntity.java` | Java | Role Entity — JPA entity (maps to a database table). |
| `RolePermissionEntity.java` | Java | Role Permission Entity — JPA entity (maps to a database table). |
| `UserRoleEntity.java` | Java | User Role Entity — JPA entity (maps to a database table). |

---
_Part of the Tamil Nadu AVGC-XR portal. This guide describes files as documented in their source headers; for authoritative behaviour, read the code._
