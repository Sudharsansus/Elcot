# `apps/api/src/main/java/in/elcot/avgcxr/common/infrastructure/security`

<!-- gen-folder-docs -->
> Folder guide for knowledge transfer — auto-generated from each file's own header documentation.
> Regenerate with `node scripts/gen-folder-docs.mjs`. Do not edit by hand (changes are overwritten); to keep a hand-written note, remove the `<!-- gen-folder-docs -->` marker line.

**Purpose:** Folder holding the files listed below (part of `apps/api/src/main/java/in/elcot/avgcxr/common/infrastructure`).

## Files (8)

| File | Type | What it does |
| --- | --- | --- |
| `AuthRateLimitFilter.java` | Java | Auth Rate Limit Filter — filter component. |
| `BlindIndexService.java` | Java | Blind Index Service — service (business logic). |
| `ChatSafetyGuard.java` | Java | Chat Safety Guard — guard component. |
| `EncryptedLocalDateConverter.java` | Java | Encrypted Local Date Converter — converter component. |
| `EncryptedStringConverter.java` | Java | Encrypted String Converter — converter component. |
| `JwtAuthenticationFilter.java` | Java | Jwt Authentication Filter — filter component. |
| `RbacExpressionHandler.java` | Java | Rbac Expression Handler — handler component. |
| `SecurityConfig.java` | Java | Security Config — Spring configuration. |

## Subfolders (1)

- [`sso/`](sso/README.md) — Folder holding the files listed below (part of `apps/api/src/main/java/in/elcot/avgcxr/common/infrastructure/security`).

---
_Part of the Tamil Nadu AVGC-XR portal. This guide describes files as documented in their source headers; for authoritative behaviour, read the code._
